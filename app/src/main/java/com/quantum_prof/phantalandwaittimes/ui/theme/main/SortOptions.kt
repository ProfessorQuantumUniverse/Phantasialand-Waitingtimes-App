package com.quantum_prof.phantalandwaittimes.ui.theme.main

enum class SortType {
    NAME,
    WAIT_TIME
}

enum class SortDirection {
    ASCENDING, // Aufsteigend (A-Z, niedrigste Zeit)
    DESCENDING // Absteigend (Z-A, höchste Zeit)
}